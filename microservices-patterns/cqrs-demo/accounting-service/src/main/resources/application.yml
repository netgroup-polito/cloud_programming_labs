server:
  port: 8080

spring:
  application:
    name: accounting-service
  datasource:
    url: jdbc:postgresql://localhost:5432/accountingdb
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

logging:
  level:
    com.example.accountingservice: DEBUG
